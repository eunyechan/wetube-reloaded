extends base


block content
    if errorMessage
        span=errorMessage
    form(method="POST")
        input(placeholder="아이디" name="username", type="text", required)
        input(placeholder="비밀번호" name="password", type="password", required)
        input.login-button(type="submit", value="Login")
        include partials/social-login
    div.auth-switch
        span 회원이 아니신가요?  
        br
        a.auth__switch-signup(href="/join")
            span 회원가입 하러가기 &rarr;
