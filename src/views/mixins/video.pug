mixin video(video)
   div
        h4
            a(href=`/videos/${video.id}`)=video.title
        ul 
            li #{video.rating}/5
            li #{video.commetns} commetns.
            li Posted #{video.createdAt}
            lii #{video.views} views.